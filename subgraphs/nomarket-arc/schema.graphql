type Market @entity(immutable: false) {
  id: ID!
  marketId: BigInt!
  creator: Bytes!
  title: String!
  metadata: String!
  atomCount: Int!
  resolved: Boolean!
  outcomeVector: BigInt!
  assertionId: Bytes
  tradingEndTime: BigInt!
  eventOccurrenceTime: BigInt!
  resolutionTime: BigInt!
  creatorDeposit: BigInt!
  totalStake: BigInt!
  totalFees: BigInt!
  rewardPool: BigInt!
  betCount: BigInt!
  creatorFeesPaid: Boolean!
  createdAtBlock: BigInt!
  createdAtTimestamp: BigInt!
  transactionHash: Bytes!
  bets: [Bet!]! @derivedFrom(field: "market")
  resolutions: [ResolutionProposal!]! @derivedFrom(field: "market")
}

type Bet @entity(immutable: true) {
  id: ID!
  market: Market!
  marketId: BigInt!
  betId: BigInt!
  bettor: Bytes!
  stake: BigInt!
  publicStake: BigInt!
  fee: BigInt!
  outcomeMask: BigInt!
  careMask: BigInt!
  expression: String!
  claimed: Boolean!
  payout: BigInt!
  transactionHash: Bytes!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  minterms: [BetMinterm!]! @derivedFrom(field: "bet")
}

type BetMinterm @entity(immutable: true) {
  id: ID!
  market: Market!
  bet: Bet!
  marketId: BigInt!
  betId: BigInt!
  mintermIndex: Int!
  outcomeMask: BigInt!
  careMask: BigInt!
}

type ResolutionProposal @entity(immutable: true) {
  id: ID!
  market: Market!
  marketId: BigInt!
  assertionId: Bytes!
  outcomeVector: BigInt!
  claim: String!
  transactionHash: Bytes!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
}

type MarketResolution @entity(immutable: true) {
  id: ID!
  market: Market!
  marketId: BigInt!
  outcomeVector: BigInt!
  transactionHash: Bytes!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
}
